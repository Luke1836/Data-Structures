//Program to print a string in a  zig-zag format, similiar to a wave array
#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    string str, newStr;
    cout << "Enter a string" << endl;
    cin >> str;
    int len = str.length();

    for(int i = 0 ; i < len ; i++)
    {
        
    }
}